# Asset Structure

Presentations are directories of numbered topics and panels.

- Topics load in numeric order (`01-intro`, `02-demo`, …)
- Each topic contains numbered panel directories (`01`, `02`, …)
- Each panel holds one or more asset files
- Panels can mix asset types — e.g. `text.md` + `prompt.md` together
