# prompt.md — Claude Prompt

A prompt staged and sent to Claude during the presentation.

- Shown in the lower portion of the screen
- Press `s` to start a countdown, then it auto-types into iTerm2
- Falls back to clipboard copy if iTerm2 is unavailable
- The first heading is the label shown in the UI
